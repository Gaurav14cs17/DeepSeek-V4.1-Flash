# Data for toy_v4_flash

Same layout as `toy_dsv41/data/`.

- `corpus_toy.txt` — builtin (`--dataset toy`)
- `opensource/` — symlinks to `toy_dsv41/data/opensource/` when present
- Download: `python -m toy_v4_flash.data.download_opensource tinystories`
